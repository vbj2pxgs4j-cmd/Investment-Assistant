# Frontend Environment Configuration
# For local development with Vite dev server proxying, you can leave this empty (defaults to http://localhost:8000 via proxy)
# Or set explicitly to your FastAPI backend URL:
VITE_API_BASE_URL=http://localhost:8000
